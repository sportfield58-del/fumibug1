# @fumibug/db

Dueño: Claude Code (ver `CLAUDE.md` §3).

Placeholder de Fase 0. El contenido real llega en:

- **PR 3** — `prisma/schema.prisma` completo, migración inicial, triggers append-only
  (`audit_logs`, `cash_movements`, `inventory_movements`), policies de RLS.
- **PR 4** — `prisma/seed.ts`.

Ver `docs/spec/08-modelo-datos.md` y `docs/spec/16-estructura.md`.
