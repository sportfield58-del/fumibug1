{
  "name": "@fumibug/contracts",
  "private": true,
  "version": "0.1.0",
  "description": "Schemas Zod = contrato con el frontend (ADR 0005). Enums del sistema, catálogo de ErrorCode, tipos de respuesta estándar y schemas de auth/entidades base. Los schemas de cada módulo se agregan en su propio PR en Fase 1.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "jest"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@fumibug/config": "workspace:*",
    "@types/jest": "^29.5.13",
    "@types/node": "^22.7.5",
    "eslint": "^9.12.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
